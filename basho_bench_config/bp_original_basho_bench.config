
{mode, max}.

{duration, minutes}.

{concurrent, 90}.

{operations, [{get, 1},{update, 1},{insert, put,1}]}.

{driver, basho_bench_driver_riakc_pb}.

{key_generator, {int_to_bin, {uniform_int, 100000000}}}.

{value_generator, {fixed_bin, 1000}}.

{report_interval, report_interval_seconds}.

{riakc_pb_ips, [
 "127.0.0.1"
]}.

