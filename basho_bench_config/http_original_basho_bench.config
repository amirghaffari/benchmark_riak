
{mode, max}.

{duration, minutes}.

{concurrent, 90}.

{operations, [{get, 1},{update, 1},{insert,1}]}.

{driver, basho_bench_driver_http_raw}.

{uniform_int, 100000000}.

{value_generator, {fixed_bin, 1000}}.

{http_raw_port, 8098}.

{report_interval, report_interval_seconds}.

{http_raw_ips, [
 "127.0.0.1"
]}.

{http_raw_path, "/riak/bench"}.

